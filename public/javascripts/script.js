document.addEventListener('DOMContentLoaded', function() {
	M.AutoInit();

	// document.getElementById('recipe-edit').onclick = () => {
	// 	console.log('test');
	// };
	// let elems = document.querySelectorAll('.modal');
	// let instances = M.Modal.init(elems, {});

	// let navbar = document.querySelectorAll('.sidenav');
	// let instances2 = M.Sidenav.init(navbar, {});

	// let carousel = document.querySelectorAll('.carousel');
	// var instances3 = M.Carousel.init(carousel, { fullWidth: true });

	var slider = document.querySelectorAll('.slider');
	var instances4 = M.Slider.init(slider, {});

	// var selects = document.querySelectorAll('select');
	// var instances5 = M.FormSelect.init(selects, {});
});
